/*
Shipeasy Admin API

REST API for managing feature gates, experiments, configs, universes, and killswitches in a Shipeasy project. Authenticate with an admin SDK key (`Authorization: Bearer sdk_admin_…`) and scope every request to a project via the `X-Project-Id` header.  Mint admin keys via `POST /api/admin/keys` with `type: \"admin\"`. Keys expire after 90 days; rotate with the `revoke` action.

API version: 2.0.0
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package admin

import (
	"encoding/json"
	"fmt"
)

// OpsItemStatus Lifecycle status of a queue item. The working flow is `open` → `in_progress` → `ready_for_qa` → `resolved` (or `wont_fix`, terminal from any earlier stage). `blocked` marks an item that can't progress until an external dependency clears — a working state a human sets and clears. `ready_for_qa` is what a developer sets once a fix lands; `resolved` is the QA sign-off, normally flipped in the dashboard after verification — set it directly from code only when the fix has been verified end-to-end. `investigating_by_ai` is a system-owned display state — set when the AI agent (Jarvis) picks an item up to investigate, never chosen by a human — so it is shown but not offered as a manual choice. `pending_approval` is the one human-gated holding state: it parks an item OUT of the work queue until a human promotes it to `open` in the dashboard, so `GET /api/admin/ops` excludes it under `status=all`/default and returns it only when requested as an exact `status`. It covers untriaged inbound that must never be auto-implemented — connector requests filed from a customer's connectors panel, and questions funnelled in from the \"Stuck in onboarding?\" assistant — where approving means flipping the status to `open`. Two earlier values were removed in favour of this single gate: `triage` (the onboarding-help bucket, now `pending_approval`) and `triaged` (a redundant \"looked at but not started\" step, now plain `open`).
type OpsItemStatus string

// List of OpsItemStatus
const (
	OPSITEMSTATUS_OPEN OpsItemStatus = "open"
	OPSITEMSTATUS_PENDING_APPROVAL OpsItemStatus = "pending_approval"
	OPSITEMSTATUS_INVESTIGATING_BY_AI OpsItemStatus = "investigating_by_ai"
	OPSITEMSTATUS_IN_PROGRESS OpsItemStatus = "in_progress"
	OPSITEMSTATUS_BLOCKED OpsItemStatus = "blocked"
	OPSITEMSTATUS_READY_FOR_QA OpsItemStatus = "ready_for_qa"
	OPSITEMSTATUS_RESOLVED OpsItemStatus = "resolved"
	OPSITEMSTATUS_WONT_FIX OpsItemStatus = "wont_fix"
)

// All allowed values of OpsItemStatus enum
var AllowedOpsItemStatusEnumValues = []OpsItemStatus{
	"open",
	"pending_approval",
	"investigating_by_ai",
	"in_progress",
	"blocked",
	"ready_for_qa",
	"resolved",
	"wont_fix",
}

func (v *OpsItemStatus) UnmarshalJSON(src []byte) error {
	var value string
	err := json.Unmarshal(src, &value)
	if err != nil {
		return err
	}
	enumTypeValue := OpsItemStatus(value)
	for _, existing := range AllowedOpsItemStatusEnumValues {
		if existing == enumTypeValue {
			*v = enumTypeValue
			return nil
		}
	}

	return fmt.Errorf("%+v is not a valid OpsItemStatus", value)
}

// NewOpsItemStatusFromValue returns a pointer to a valid OpsItemStatus
// for the value passed as argument, or an error if the value passed is not allowed by the enum
func NewOpsItemStatusFromValue(v string) (*OpsItemStatus, error) {
	ev := OpsItemStatus(v)
	if ev.IsValid() {
		return &ev, nil
	} else {
		return nil, fmt.Errorf("invalid value '%v' for OpsItemStatus: valid values are %v", v, AllowedOpsItemStatusEnumValues)
	}
}

// IsValid return true if the value is valid for the enum, false otherwise
func (v OpsItemStatus) IsValid() bool {
	for _, existing := range AllowedOpsItemStatusEnumValues {
		if existing == v {
			return true
		}
	}
	return false
}

// Ptr returns reference to OpsItemStatus value
func (v OpsItemStatus) Ptr() *OpsItemStatus {
	return &v
}

type NullableOpsItemStatus struct {
	value *OpsItemStatus
	isSet bool
}

func (v NullableOpsItemStatus) Get() *OpsItemStatus {
	return v.value
}

func (v *NullableOpsItemStatus) Set(val *OpsItemStatus) {
	v.value = val
	v.isSet = true
}

func (v NullableOpsItemStatus) IsSet() bool {
	return v.isSet
}

func (v *NullableOpsItemStatus) Unset() {
	v.value = nil
	v.isSet = false
}

func NewNullableOpsItemStatus(val *OpsItemStatus) *NullableOpsItemStatus {
	return &NullableOpsItemStatus{value: val, isSet: true}
}

func (v NullableOpsItemStatus) MarshalJSON() ([]byte, error) {
	return json.Marshal(v.value)
}

func (v *NullableOpsItemStatus) UnmarshalJSON(src []byte) error {
	v.isSet = true
	return json.Unmarshal(src, &v.value)
}

